# Tasks
- [x] `scripts/build-pages.mjs`: export main and each open pull request, preview changes, listing, data file, command line
- [x] Pages workflow: push to main, pull request events through `pull_request_target`, manual run; read-only permissions plus Pages; whole-site artifact
- [x] Live app: footer link from the build's marker, hidden anywhere else
- [x] `test/pages.test.mjs` in `npm test`: summaries, escaping, a built site in a browser (listing, preview isolation, shared samples, live app)
- [x] README section, version 3.0.3
- [x] Deploy, and check `builds/` and `pr/1/` on the live site
- [x] 3.0.4: the live service worker leaves `pr/` and `builds/` to the network, with a browser test
