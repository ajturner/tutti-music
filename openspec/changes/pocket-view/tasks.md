# Tasks
- [x] `src/ui/pocket.js`: on/off (View toggle, `?pocket`, remembered), the five levels, the menu, the sound list, rendering per frame, the on-screen pad and keys through `state.padHeld`/`state.padTaps`
- [x] `pollGamepad` merges the pad with a physical controller and hands the pocket levels to `pocketInput`
- [x] Markup, styles (four bands, pad), View toggle; the draw loop renders the pocket instead of the grid
- [x] Browser tests at phone size: toggle, song, phrase entry and edit, play, levels out, instruments, menu, back to full
- [x] Guide, README, specs
